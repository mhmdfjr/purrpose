// Placeholder for taskCutoverJob (M3)
// Will query users where utcResetHour == currentHour and transition pending tasks to missed.
// See ARCHITECTURE.md 4.2 and DATABASE.md Section 3 for design.

// import { onSchedule } from "firebase-functions/v2/scheduler";
// export const taskCutoverJob = onSchedule("every hour", async () => {});
export {};
